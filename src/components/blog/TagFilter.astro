---
interface Props {
	tags: string[];
}

const { tags } = Astro.props;
---

{
	tags.length > 0 ? (
		<div class="tag-filter" role="group" aria-label="Filter posts by tag">
			<button type="button" class="filter-pill is-active" data-tag="" aria-pressed="true">
				All
			</button>
			{tags.map((tag) => (
				<button type="button" class="filter-pill" data-tag={tag} aria-pressed="false">
					{tag}
				</button>
			))}
		</div>
	) : null
}

<style lang="scss">
	@use "../../style/pill" as *;

	.tag-filter {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
		margin-bottom: 1.5rem;
	}

	.filter-pill {
		@include pill;
		padding: 0.45em 0.7em;
		background: transparent;
		cursor: pointer;

		&.is-active {
			background-color: var(--text-color-link);
			color: var(--background-color);
			font-weight: bold;
		}
	}
</style>
