---

---

<nav class="nav-links">
    <a href="/">Home</a>
    <a href="/about/">About</a>
    <a href="/credits/">Credit</a>
</nav>

<style>
    .nav-links {
        margin-left: 5em;
        display: block;
        position: static;
        width: auto;
        background: none;
    }

    .nav-links a {
        display: inline-block;
        padding: 15px 20px;
    }

    @media (max-width: 1024px) {
        .nav-links {
            width: 100%;
            top: 5rem;
            display: none;
            position: absolute;
        }

        .nav-links a {
            display: block;
            text-align: center;
            padding: 10px 0;
            text-decoration: none;
            font-size: 1.2rem;
            font-weight: bold;
            color: var(--text-color-aside);
            background-color: var(--turquoise);
            border-top: 1px solid var(--text-color-aside);
        }

        .nav-links a:hover,
        .nav-links a:focus {
            text-decoration: underline;
        }

        .expanded {
            display: unset;
        }
    }
</style>
